class Rewards_Model {
  String id, title, description;
  int price;

  Rewards_Model(
      {required this.id,
        required this.title,
        required this.description,
        required this.price});
}
